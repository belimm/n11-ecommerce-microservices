spring:
  datasource:
    url: ${STOCK_DB_URL:${DB_URL}}
    username: ${STOCK_DB_USERNAME:${DB_USERNAME}}
    password: ${STOCK_DB_PASSWORD:${DB_PASSWORD}}

app:
  seed:
    enabled: ${STOCK_SEED_ENABLED:true}
